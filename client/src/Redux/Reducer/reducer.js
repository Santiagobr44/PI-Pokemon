// * Importas las actions-types

// * Definir el initialState
let initialState = {
  allPokemons: [],
  allPowers: [],
};

// * Definir la funcion rootReducer

function rootReducer(state = initialState, action) {}

export default rootReducer;
